/**
 * googleApi.ts — STUB
 *
 * All Google API calls (Calendar, Gmail, Tasks) are now handled exclusively
 * by Firebase Cloud Functions. This file is intentionally empty.
 *
 * Do NOT call Google APIs directly from browser code.
 * Use the typed wrappers in `@/lib/functions.ts` instead.
 */
export {};
